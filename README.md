# json-db
data consumindo json

```
Para localhost
"scripts": {
    "start": "json-server --watch db.json --port 3001",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },

Para heroku
  "scripts": {    
    "start": "node server.js",
    "dev": "nodemon server.js"
  },

 ```
