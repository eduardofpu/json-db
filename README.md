# json-db
data consumindo json

```
https://github.com/jesperorb/json-server-heroku/blob/master/package.json

Para localhost
"scripts": {
    "start": "json-server --watch db.json --port 3001"    
  },

Para heroku
  "scripts": {    
    "start": "node server.js",
    "dev": "nodemon server.js"
  },

 ```
